# Coleta das notícias
gnews
pytz

# Extração do conteúdo das páginas
requests
beautifulsoup4

# Configuração e resiliência
python-dotenv
tenacity

# Provedores de IA
# `openai` atende Groq, Gemini, OpenAI, OpenRouter, DeepSeek, Mistral,
# Azure OpenAI e Ollama (todos expõem endpoints compatíveis).
openai
# Necessário apenas se LLM_PROVIDER=anthropic:
anthropic

# Microsoft Graph (envio de e-mail) e SharePoint (preferências)
msal
